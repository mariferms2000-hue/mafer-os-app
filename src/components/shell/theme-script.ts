/** Se ejecuta antes del primer render (inline en <head>) para evitar flash de tema. */
export const THEME_INIT_SCRIPT = `(function(){try{var t=localStorage.getItem('mafer-theme')||'light';var d=t==='auto'?(matchMedia('(prefers-color-scheme: dark)').matches?'dark':'light'):t;var h=document.documentElement;h.setAttribute('data-theme',d);h.setAttribute('data-theme-pref',t);var m=document.querySelector('meta[name="theme-color"]');if(m)m.setAttribute('content',d==='dark'?'#1e231b':'#faf7f1');}catch(e){}})();`;
